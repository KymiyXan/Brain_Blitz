<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Brain_Blitz</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        /* Background Image */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(45deg, #d67eff, #ff75ef, #b87eff, #ff758c);
            background-size: 300% 300%;
            animation: moveBackground 6s infinite alternate;
        }

        @keyframes moveBackground {
            0% {
                background-position: 0% 50%;
            }
            100% {
                background-position: 100% 50%;
            }
        }

        /* Glassmorphism Effect */
        .card {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 20px;
            width: 400px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            color: white;
        }

        /* Form Fields */
        .form-control {
            background: rgba(255, 255, 255, 0.3);
            color: white;
            border: none;
        }

        .form-control::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        /* Improved Button Style */
        .btn-primary {
            background-color: #ff0000;
            border: none;
            font-weight: bold;
            transition: 0.3s;
        }

        .btn-primary:hover {
            background-color: #8900b3;
        }

        /* Text Color */
        h2, label, p {
            color: white;
        }

        /* Register Link */
        .register-link {
            color: rgb(64, 0, 255);
            text-decoration: none;
        }

        .register-link:hover {
            text-decoration: underline;
        }
        </style>
</head>
<body>

    <div class="container text-center mt-5">
        <h1 class="fw-bold">Welcome to Brain_Blitz</h1>
        <p class="lead text-muted">Blitz. Blitz. Blitz. Brain_Blitz</p>
        
        <div class="mt-4">
            <a href="/login" class="btn btn-primary btn-lg mx-2">Login</a>
            <a href="/register" class="btn btn-success btn-lg mx-2">Register</a>
        </div>
    </div>

</body>
</html>

